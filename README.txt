First Coding Exam

Data Access Layer:
	It Contains 2 files for each Model.
	One file is interface showing methods & another file is it's implementation for storing the objects in temporary database.

	
Business Logic Layer:
	It Contains 2 files for each Model.
	One file is interface showing methods & another file is it's implementation for calling the dao methods.


View Layer:
	It contains the main Controller file that runs the program.


Entity Classes:
	It contains 3 model classes for each category.


Exception:
	It contains 2 user defined exception